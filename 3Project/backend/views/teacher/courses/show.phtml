<?php

$course ??= null;
$students ??= [];
$exams ??= [];
$studentCount ??= count($students);

$pageTitle = $course['course_name'] ?? 'Course Details';
$activePage = 'courses';

require __DIR__ . '/../../partials/header.phtml';

?>

<main class="container py-5">

    <?php if ($course === null): ?>
        <div class="alert alert-danger" role="alert">
            Course not found.
        </div>
    <?php else: ?>

        <div class="d-flex flex-column flex-lg-row justify-content-between align-items-lg-start gap-3 mb-4">

            <div>
                <span class="badge text-bg-primary mb-2">
                    <?= htmlspecialchars(
                        $course['course_code'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </span>

                <h1 class="h2 mb-1">
                    <?= htmlspecialchars(
                        $course['course_name'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </h1>

                <p class="text-body-secondary mb-0">
                    Manage this course, its students, exams and questions.
                </p>
            </div>

            <a
                href="/newSummerTraining/3Project/backend/dashboard"
                class="btn btn-outline-secondary"
            >
                Back to dashboard
            </a>

        </div>

        <div class="row g-3 mb-4">

            <div class="col-12 col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <p class="text-body-secondary mb-2">
                            Enrolled students
                        </p>

                        <p class="display-6 fw-semibold mb-0">
                            <?= (int) $studentCount ?>
                        </p>
                    </div>
                </div>
            </div>

            <div class="col-12 col-md-6">
                <div class="card shadow-sm h-100">
                    <div class="card-body">
                        <p class="text-body-secondary mb-2">
                            Course exams
                        </p>

                        <p class="display-6 fw-semibold mb-0">
                            <?= count($exams) ?>
                        </p>
                    </div>
                </div>
            </div>

        </div>

        <div class="card shadow-sm mb-4">
            <div class="card-body p-4">

                <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-center gap-3">

                    <div>
                        <h2 class="h4 mb-1">Course actions</h2>

                        <p class="text-body-secondary mb-0">
                            Create exams or manage the question bank.
                        </p>
                    </div>

                    <div class="d-flex flex-wrap gap-2">

                        <a
                            href="/newSummerTraining/3Project/backend/teacher/courses/exams/create/<?= (int) $course['id'] ?>"
                            class="btn btn-primary"
                        >
                            Create exam
                        </a>

                        <a
                            href="/newSummerTraining/3Project/backend/api/courses/questions/<?= (int) $course['id'] ?>"
                            class="btn btn-outline-primary"
                        >
                            Open question bank
                        </a>

                    </div>
                </div>

            </div>
        </div>

        <div class="card shadow-sm mb-4">

            <div class="card-header bg-body py-3">
                <h2 class="h4 mb-0">Exams</h2>
            </div>

            <?php if ($exams === []): ?>

                <div class="card-body">
                    <div class="alert alert-info mb-0" role="alert">
                        No exams have been created for this course yet.
                    </div>
                </div>

            <?php else: ?>

                <div class="list-group list-group-flush">

                    <?php foreach ($exams as $exam): ?>
                        <?php
                        $status = ucfirst($exam['status']);

                        $badgeClass = match ($exam['status']) {
                            'upcoming' => 'text-bg-warning',
                            'available' => 'text-bg-success',
                            default => 'text-bg-secondary'
                        };
                        ?>

                        <div class="list-group-item p-4">

                            <div class="row align-items-center g-3">

                                <div class="col">

                                    <div class="d-flex align-items-center gap-2 mb-2">
                                        <h3 class="h5 mb-0">
                                            <?= htmlspecialchars(
                                                $exam['exam_name'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </h3>

                                        <span class="badge <?= $badgeClass ?>">
                                            <?= $status ?>
                                        </span>
                                    </div>

                                    <div class="small text-body-secondary">
                                        <div>
                                            <span class="fw-semibold">Starts:</span>

                                            <?= htmlspecialchars(
                                                $exam['start_time'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </div>

                                        <div>
                                            <span class="fw-semibold">Ends:</span>

                                            <?= htmlspecialchars(
                                                $exam['end_time'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </div>
                                    </div>

                                </div>

                                <div class="col-12 col-md-auto">
                                    <a
                                        href="/newSummerTraining/3Project/backend/teacher/exams/<?= (int) $exam['id'] ?>"
                                        class="btn btn-outline-primary btn-sm"
                                    >
                                        View exam
                                    </a>
                                </div>

                            </div>

                        </div>
                    <?php endforeach; ?>

                </div>

            <?php endif; ?>

        </div>

        <div class="card shadow-sm">

            <div class="card-header bg-body py-3">
                <h2 class="h4 mb-0">
                    Enrolled students
                </h2>
            </div>

            <?php if ($students === []): ?>

                <div class="card-body">
                    <div class="alert alert-info mb-0" role="alert">
                        No students are enrolled in this course yet.
                    </div>
                </div>

            <?php else: ?>

                <div class="table-responsive">
                    <table class="table table-hover align-middle mb-0">

                        <thead class="table-light">
                            <tr>
                                <th scope="col">Student</th>
                                <th scope="col">Student ID</th>
                                <th scope="col">Email</th>
                            </tr>
                        </thead>

                        <tbody>
                            <?php foreach ($students as $student): ?>
                                <tr>
                                    <td class="fw-semibold">
                                        <?= htmlspecialchars(
                                            $student['name'],
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </td>

                                    <td>
                                        <?= htmlspecialchars(
                                            $student['personal_id'],
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </td>

                                    <td>
                                        <?= htmlspecialchars(
                                            $student['email'],
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </td>
                                </tr>
                            <?php endforeach; ?>
                        </tbody>

                    </table>
                </div>

            <?php endif; ?>

        </div>

    <?php endif; ?>

</main>

<?php require __DIR__ . '/../../partials/footer.phtml'; ?>
