<?php

$questions ??= [];
$courseId ??= 0;
$course ??= null;

$pageTitle = 'Question Bank';
$activePage = 'courses';

require __DIR__ . '/../partials/header.phtml';

?>

<main class="container py-5">

    <div class="d-flex justify-content-between align-items-start mb-4">
        <div>
            <h1 class="h2 mb-1">Question Bank</h1>

            <?php if ($course !== null): ?>
                <p class="text-body-secondary mb-0">
                    <?= htmlspecialchars(
                        $course['course_code'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>

                    —

                    <?= htmlspecialchars(
                        $course['course_name'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </p>
            <?php endif; ?>
        </div>

        <a
            href="/newSummerTraining/3Project/backend/api/courses/<?= (int) $courseId ?>/questions/create"
            class="btn btn-primary"
        >
            Add question
        </a>
    </div>

    <?php if ($questions === []): ?>

        <div class="alert alert-info" role="alert">
            This course does not have any questions yet.
        </div>

    <?php else: ?>

        <div class="card shadow-sm">
            <div class="list-group list-group-flush">

                <?php foreach ($questions as $index => $question): ?>
                    <?php
                    $questionType =
                        $question['question_type'] ?? 'MCQ';

                    $typeLabel =
                        $questionType === 'TrueOrFalse'
                            ? 'True / False'
                            : 'Multiple Choice';

                    $typeClass =
                        $questionType === 'TrueOrFalse'
                            ? 'text-bg-info'
                            : 'text-bg-primary';
                    ?>

                    <div class="list-group-item p-4">
                        <div class="row align-items-center g-3">

                            <div class="col">
                                <div class="d-flex align-items-center gap-2 mb-2">
                                    <span class="badge text-bg-secondary">
                                        #<?= $index + 1 ?>
                                    </span>

                                    <span class="badge <?= $typeClass ?>">
                                        <?= $typeLabel ?>
                                    </span>
                                </div>

                                <h2 class="h5 mb-0">
                                    <?= htmlspecialchars(
                                        $question['question_text'],
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>
                                </h2>
                            </div>

                            <div class="col-12 col-lg-auto">
                                <div class="d-flex flex-wrap gap-2">

                                    <a
                                        href="/newSummerTraining/3Project/backend/api/questions/<?= (int) $question['id'] ?>/edit"
                                        class="btn btn-outline-secondary btn-sm"
                                    >
                                        Edit
                                    </a>

                                    <form
                                        method="POST"
                                        action="/newSummerTraining/3Project/backend/api/questions/<?= (int) $question['id'] ?>/delete"
                                        class="d-inline"
                                    >
                                        <button
                                            type="submit"
                                            class="btn btn-outline-danger btn-sm"
                                            onclick="return confirm('Delete this question?')"
                                        >
                                            Delete
                                        </button>
                                    </form>

                                </div>
                            </div>

                        </div>
                    </div>
                <?php endforeach; ?>

            </div>
        </div>

    <?php endif; ?>

    <div class="mt-4">
        <a
            href="/newSummerTraining/3Project/backend/teacher/courses/<?= (int) $courseId ?>"
            class="btn btn-outline-secondary"
        >
            Back to course
        </a>
    </div>

</main>

<?php require __DIR__ . '/../partials/footer.phtml'; ?>