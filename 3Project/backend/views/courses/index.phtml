<?php

$courses ??= [];
$availableCourses ??= [];
$search ??= '';

$pageTitle = 'Courses';
$activePage = 'courses';

require __DIR__ . '/../partials/header.phtml';

?>

<main class="container py-5">

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="h2 mb-1">Courses</h1>

                <p class="text-body-secondary mb-0">
                    Manage your courses and question banks.
                </p>
            </div>

            <a
                href="/newSummerTraining/3Project/backend/api/courses/create"
                class="btn btn-primary">
                Add a new course
            </a>
        </div>

        <?php if ($courses === []): ?>
            <div class="alert alert-info" role="alert">
                No courses were found. Create your first course to get started.
            </div>
        <?php else: ?>
            <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
                <?php foreach ($courses as $course): ?>
                    <a href="/newSummerTraining/3Project/backend/teacher/courses/<?= (int) $course['id'] ?>" class="text-decoration-none">
                        <div class="col">
                            <div class="card h-100 shadow-sm">
                                <div class="card-body">
                                    <span class="badge text-bg-primary mb-3">
                                        <?= htmlspecialchars(
                                            $course['course_code'],
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </span>

                                    <h2 class="card-title h5">
                                        <?= htmlspecialchars(
                                            $course['course_name'],
                                            ENT_QUOTES,
                                            'UTF-8'
                                        ) ?>
                                    </h2>

                                    <p class="card-text text-body-secondary">
                                        View and manage this course.
                                    </p>
                                </div>

                            </div>
                        </div>
                    </a>
                <?php endforeach; ?>
            </div>
        <?php endif; ?>


        <hr>

        <div class="d-flex justify-content-between align-items-center mb-4">
            <div class="w-100">
                <h1 class="h2 mb-1">Co-Teach other Courses</h1>

                <p class="text-body-secondary ">
                    Manage other courses and question banks.
                </p>



                <form method="GET" action="" class="row g-2 w-50 mb-3">
                    <div class="col">
                        <input
                            type="search"
                            name="search"
                            class="form-control"
                            placeholder="Search by name or code"
                            value="<?= htmlspecialchars(
                                        $search,
                                        ENT_QUOTES,
                                        'UTF-8'
                                    ) ?>">
                    </div>

                    <div class="col-auto">
                        <button type="submit" class="btn btn-primary mx-2">
                            Search
                        </button>
                    </div>
                </form>

                <?php if ($availableCourses === []): ?>

                    <div class="alert alert-light border">
                        No available courses match your search.
                    </div>

                <?php else: ?>

                    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">

                        <?php foreach ($availableCourses as $availableCourse): ?>

                            <div class="col">
                                <div class="card h-100 shadow-sm">
                                    <div class="card-body">
                                        <span class="badge text-bg-primary mb-3">
                                            <?= htmlspecialchars(
                                                $availableCourse['course_code'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </span>

                                        <h2 class="card-title h5">
                                            <?= htmlspecialchars(
                                                $availableCourse['course_name'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </h2>

                                        <div class="card-footer bg-white border-0 p-4">
                                            <form
                                                method="POST"
                                                action="/newSummerTraining/3Project/backend/teacher/courses/co-teach/<?= (int) $availableCourse['id'] ?>">
                                                <button
                                                    type="submit"
                                                    class="btn btn-outline-primary w-100">
                                                    Co-teach this course
                                                </button>
                                            </form>
                                        </div>
                                    </div>

                                </div>
                            </div>

                        <?php endforeach; ?>

                    </div>

                <?php endif; ?>
            </div>
        </div>




</main>

<?php require __DIR__ . '/../partials/footer.phtml'; ?>
