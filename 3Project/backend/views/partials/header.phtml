<?php

$pageTitle ??= 'Quiz System';
$activePage ??= '';
$showNavigation ??= isset($_SESSION['user_id']);
$myCoursesUrl =
    ($_SESSION['role'] ?? '') === 'student'
        ? '/newSummerTraining/3Project/backend/student/courses'
        : '/newSummerTraining/3Project/backend/api/courses';


?>

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">

    <meta
        name="viewport"
        content="width=device-width, initial-scale=1">

    <title>
        <?= htmlspecialchars(
            $pageTitle,
            ENT_QUOTES,
            'UTF-8'
        ) ?>
    </title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
        rel="stylesheet">
</head>

<body class="bg-body-tertiary">

    <nav class="navbar navbar-expand-lg bg-dark navbar-dark">
        <div class="container">
            <a
                class="navbar-brand fw-bold"
                href="<?= $showNavigation
                            ? '/newSummerTraining/3Project/backend/dashboard'
                            : '/newSummerTraining/3Project/backend/login'
                        ?>">
                Quiz System
            </a>

            <?php

            if ($showNavigation) {
            ?>
                <div class="navbar-nav">
                    <a
                        class="nav-link <?= $activePage === 'home'
                            ? 'active'
                            : '' ?>"
                        href="/newSummerTraining/3Project/backend/home">
                        Home
                    </a>

                    <a
                        class="nav-link <?= $activePage === 'dashboard'
                            ? 'active'
                            : '' ?>"
                        href="/newSummerTraining/3Project/backend/dashboard">
                        Dashboard
                    </a>

                    <a
                        class="nav-link <?= $activePage === 'courses'
                            ? 'active'
                            : '' ?>"
                        href="<?= $myCoursesUrl ?>">
                        My Courses
                    </a>

                    <form
                        method="POST"
                        action="/newSummerTraining/3Project/backend/logout"
                        class="ms-5">
                        <button
                            type="submit"
                            class="btn btn-outline-light">
                            Logout
                        </button>
                    </form>
                </div>

            <?php
            }



            ?>
        </div>
    </nav>
