<?php

if (!isset($exam) || !is_array($exam)) {
    throw new LogicException(
        'The student exam view requires an exam.'
    );
}

$questions ??= [];
$formAction ??=
    '/newSummerTraining/3Project/backend/student/exams/'
    . (int) $exam['id']
    . '/submit';
$pageHeading ??= $exam['exam_name'];
$introText ??= 'Answer every question before submitting.';
$statusLabel ??= 'Exam available';

$pageTitle = 'Take Exam';
$activePage = 'courses';

require __DIR__ . '/../../partials/header.phtml';

?>

<main class="container py-5">
    <div class="d-flex justify-content-between align-items-start mb-4">
        <div>
            <h1 class="h2 mb-1">
                <?= htmlspecialchars($pageHeading, ENT_QUOTES, 'UTF-8') ?>
            </h1>

            <p class="text-body-secondary mb-0">
                <?= htmlspecialchars($introText, ENT_QUOTES, 'UTF-8') ?>
            </p>
        </div>

        <span class="badge text-bg-success">
            <?= htmlspecialchars($statusLabel, ENT_QUOTES, 'UTF-8') ?>
        </span>
    </div>

    <form
        method="POST"
        action="<?= htmlspecialchars($formAction, ENT_QUOTES, 'UTF-8') ?>"
    >
        <?php foreach ($questions as $index => $question): ?>
            <div class="card shadow-sm mb-4">
                <div class="card-header bg-white d-flex justify-content-between">
                    <strong>Question <?= $index + 1 ?></strong>

                    <span class="badge text-bg-primary">
                        <?= (float) $question['weight'] ?> marks
                    </span>
                </div>

                <div class="card-body">
                    <h2 class="h5 mb-4">
                        <?= htmlspecialchars(
                            $question['question_text'],
                            ENT_QUOTES,
                            'UTF-8'
                        ) ?>
                    </h2>

                    <?php foreach ($question['choices'] as $choice): ?>
                        <div class="form-check border rounded p-3 ps-5 mb-2">
                            <input
                                class="form-check-input"
                                type="<?= $question['question_type'] === 'TrueOrFalse'
                                    ? 'radio'
                                    : 'checkbox' ?>"
                                name="answers[<?= (int) $question['id'] ?>][]"
                                value="<?= (int) $choice['id'] ?>"
                                id="choice-<?= (int) $choice['id'] ?>"
                            >

                            <label
                                class="form-check-label w-100"
                                for="choice-<?= (int) $choice['id'] ?>"
                            >
                                <?= htmlspecialchars(
                                    $choice['choice_text'],
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) ?>
                            </label>
                        </div>
                    <?php endforeach; ?>
                </div>
            </div>
        <?php endforeach; ?>

        <div class="d-flex justify-content-end">
            <button type="submit" class="btn btn-primary btn-lg">
                Submit exam
            </button>
        </div>
    </form>
</main>

<?php require __DIR__ . '/../../partials/footer.phtml'; ?>
