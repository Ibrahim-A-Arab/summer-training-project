<?php

$course ??= null;
$exams ??= [];

$pageTitle = 'Course';
$activePage = 'courses';

require __DIR__ . '/../../partials/header.phtml';

?>

<main class="container py-5">

    <?php if ($course === null): ?>
        <div class="alert alert-danger" role="alert">
            Course not found.
        </div>
    <?php else: ?>

        <div class="d-flex justify-content-between align-items-start mb-4">
            <div>
                <span class="badge text-bg-primary mb-2">
                    <?= htmlspecialchars(
                        $course['course_code'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </span>

                <h1 class="h2 mb-1">
                    <?= htmlspecialchars(
                        $course['course_name'],
                        ENT_QUOTES,
                        'UTF-8'
                    ) ?>
                </h1>

                <p class="text-body-secondary mb-0">
                    View scheduled exams for this course.
                </p>
            </div>

            <a
                href="/newSummerTraining/3Project/backend/student/courses"
                class="btn btn-outline-secondary"
            >
                Back to my courses
            </a>
        </div>

        <div class="card shadow-sm">
            <div class="card-header bg-white">
                <h2 class="h5 mb-0">Course exams</h2>
            </div>

            <?php if ($exams === []): ?>
                <div class="card-body">
                    <div class="alert alert-info mb-0" role="alert">
                        No exams are currently scheduled for this course.
                    </div>
                </div>
            <?php else: ?>
                <div class="list-group list-group-flush">

                    <?php foreach ($exams as $exam): ?>
                        <?php
                        $status = $exam['status'];

                        $statusClass = match ($status) {
                            'upcoming' => 'text-bg-warning',
                            'available' => 'text-bg-success',
                            default => 'text-bg-secondary'
                        };

                        $statusLabel = match ($status) {
                            'upcoming' => 'Upcoming',
                            'available' => 'Available',
                            default => 'Ended'
                        };
                        ?>

                        <div class="list-group-item p-4">
                            <div class="row align-items-center g-3">

                                <div class="col-12 col-lg">
                                    <div class="d-flex align-items-center gap-2 mb-2">
                                        <h3 class="h5 mb-0">
                                            <?= htmlspecialchars(
                                                $exam['exam_name'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </h3>

                                        <span class="badge <?= $statusClass ?>">
                                            <?= $statusLabel ?>
                                        </span>
                                    </div>

                                    <div class="text-body-secondary small">
                                        <div>
                                            <strong>Starts:</strong>

                                            <?= htmlspecialchars(
                                                $exam['start_time'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </div>

                                        <div>
                                            <strong>Ends:</strong>

                                            <?= htmlspecialchars(
                                                $exam['end_time'],
                                                ENT_QUOTES,
                                                'UTF-8'
                                            ) ?>
                                        </div>
                                    </div>
                                </div>

                                <div class="col-12 col-lg-auto">
                                    <?php if ($status === 'available'): ?>
                                        <a
                                            href="/newSummerTraining/3Project/backend/student/exams/<?= (int) $exam['id'] ?>"
                                            class="btn btn-success"
                                        >
                                            Participate
                                        </a>
                                    <?php elseif ($status === 'upcoming'): ?>
                                        <button
                                            type="button"
                                            class="btn btn-outline-warning"
                                            disabled
                                        >
                                            Not started
                                        </button>
                                    <?php else: ?>
                                        <button
                                            type="button"
                                            class="btn btn-outline-secondary"
                                            disabled
                                        >
                                            Exam ended
                                        </button>
                                    <?php endif; ?>
                                </div>

                            </div>
                        </div>
                    <?php endforeach; ?>

                </div>
            <?php endif; ?>
        </div>

    <?php endif; ?>

</main>

<?php require __DIR__ . '/../../partials/footer.phtml'; ?>
