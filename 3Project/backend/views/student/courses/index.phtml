<?php
$courses ??= [];

$pageTitle = 'My Courses';
$activePage = 'courses';

require __DIR__ . '/../../partials/header.phtml';
?>

<main class="container py-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="h2 mb-1">My Courses</h1>

            <p class="text-body-secondary mb-0">
                You are enrolled in <?= count($courses) ?> courses.
            </p>
        </div>

        <a
            href="/newSummerTraining/3Project/backend/student/courses/enroll"
            class="btn btn-primary"
        >
            Enroll in more courses
        </a>
    </div>

    <?php if ($courses === []): ?>
        <div class="alert alert-info">
            You are not enrolled in any courses yet.
        </div>
    <?php else: ?>
        <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
            <?php foreach ($courses as $course): ?>
                <div class="col">
                    <div class="card h-100 shadow-sm">
                        <div class="card-body">
                            <span class="badge text-bg-primary mb-3">
                                <?= htmlspecialchars(
                                    $course['course_code'],
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) ?>
                            </span>

                            <h2 class="card-title h5">
                                <?= htmlspecialchars(
                                    $course['course_name'],
                                    ENT_QUOTES,
                                    'UTF-8'
                                ) ?>
                            </h2>
                        </div>

                        <div class="card-footer bg-transparent border-0 pt-0">
                            <a
                                href="/newSummerTraining/3Project/backend/student/courses/<?= (int) $course['id'] ?>"
                                class="btn btn-outline-primary w-100"
                            >
                                Open course
                            </a>
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    <?php endif; ?>
</main>

<?php require __DIR__ . '/../../partials/footer.phtml'; ?>